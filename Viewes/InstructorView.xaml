<Page x:Class="FireTestingApp_net8.Viewes.InstructorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FireTestingApp_net8.Viewes"
      xmlns:vm="clr-namespace:FireTestingApp_net8.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="640" d:DesignWidth="960"
      Title="Админ-панель">

    <Grid Background="White">

        <!--SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}-->
        <TabControl Margin="0,150,0,0">

            <!--SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"-->
            <TabItem FontSize="14" Header="Администрирование тестов">
                <TabControl FontSize="14" >

                    <TabItem Header="Результаты тестирования">
                        <Grid Background="#FFE5E5E5">
                            <!--<Button Command="{Binding ExitEvent}" Content="ВЫЙТИ" HorizontalAlignment="Right" Margin="0,303,10,0" VerticalAlignment="Top" Width="278" Height="40"/>
                            <TextBlock Text="{Binding WelcomeMessage}" HorizontalAlignment="Right" Margin="0,10,10,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="278" Height="33" TextAlignment="Center"/>-->

                            <DataGrid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HeadersVisibility="Column" CanUserResizeColumns="False" ColumnWidth="*" ItemsSource="{Binding ResultsTable}" AutoGenerateColumns="false" CanUserReorderColumns="False" IsReadOnly="True" FontSize="14">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Имя" Binding="{Binding User.Firstname}"/>
                                    <DataGridTextColumn Header="Фамилия" Binding="{Binding User.Lastname}"/>
                                    <DataGridTextColumn Header="Отчество" Binding="{Binding User.Surname}"/>
                                    <DataGridTextColumn Header="Дата сдачи" Binding="{Binding Testdate, StringFormat=dd.MM.yyy | HH:MM}"/>
                                    <DataGridTextColumn Header="Баллы" Binding="{Binding Userscore}"/>
                                    <DataGridTextColumn Header="Статус экзамена" Binding="{Binding Status.Statusname}"/>

                                    <DataGridTemplateColumn>
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Редактировать" 
                                        Background="#4CAF50"
                                        Foreground="White"
                                        Command="{Binding DataContext.EditResultEvent,
                                        RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn>
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Удалить"
                                        Background="#F44336"
                                        Foreground="White"
                                        Command="{Binding DataContext.DeleteResultEvent,
                                        RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                </DataGrid.Columns>
                            </DataGrid>


                        </Grid>
                    </TabItem>

                    <TabItem Header="Пользовательские ответы">
                        <Grid Background="#FFE5E5E5">

                            <DataGrid ItemsSource="{Binding UserAnswerTable}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                                      CanUserReorderColumns="False" CanUserResizeColumns="false" HorizontalAlignment="Stretch" ColumnWidth="*">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Имя" Binding="{Binding User.Firstname}"/>
                                    <DataGridTextColumn Header="Фамилия" Binding="{Binding User.Lastname}"/>
                                    <DataGridTextColumn Header="Отчество" Binding="{Binding User.Surname}"/>
                                    <DataGridTextColumn Header="Дата" Binding="{Binding Answerdate, StringFormat=dd.MM.yyy | HH:MM}"/>
                                    <DataGridTextColumn Header="Вопрос" Binding="{Binding Question.Questiontext}">

                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="TextWrapping" Value="Wrap"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>

                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Ответ" Binding="{Binding Answer.Answertext}"/>

                                    <DataGridTemplateColumn Header="Правильность" CanUserSort="True">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox Content="правильный" IsEnabled="false" IsChecked="{Binding Iscorrect, Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                </DataGrid.Columns>
                            </DataGrid>

                        </Grid>
                    </TabItem>

                    <TabItem x:Name="DGTickets" Header="Билеты">
                        <Grid>
                            <Button Command="{Binding AddNewQuestionEvent}" Content="Новый билет" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10" Height="44" Width="152"/>

                            <DataGrid ColumnWidth="*" CanUserReorderColumns="false" AutoGenerateColumns="False" IsReadOnly="true" ItemsSource="{Binding QuestionTable}" Margin="0,0,0,66">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ID вопроса" Binding="{Binding Questionid}" MaxWidth="85"/>

                                    <DataGridTextColumn Header="Вопрос" MinWidth="700" Binding="{Binding Questiontext}">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="TextWrapping" Value="Wrap"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTemplateColumn>
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="подробнее"
                                                        Command="{Binding DataContext.EditQuestionEvent,
                                                    RelativeSource={RelativeSource AncestorType=Page}}"
                                                        CommandParameter="{Binding}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn>
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button
                                                    Content="Удалить"
                                                    Background="#F44336"
                                                    Foreground="white"
                                                    Command="{Binding DataContext.DeleteQuestionEvent,
                                                    RelativeSource={RelativeSource AncestorType=Page}}"
                                                    CommandParameter="{Binding}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </TabItem>

                </TabControl>
            </TabItem>

            <TabItem FontSize="14" Header="Администрирование пользователей">

                <TabControl>
                    <TabItem Header="Пользователи">

                        <Grid>



                            <DataGrid ItemsSource="{Binding UserTable}" AutoGenerateColumns="false" IsReadOnly="true" VerticalAlignment="Top" Height="413">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Имя" Binding="{Binding Firstname}"/>
                                    <DataGridTextColumn Header="Фамилия" Binding="{Binding Lastname}"/>
                                    <DataGridTextColumn Header="Отчество" Binding="{Binding Surname}"/>
                                    <DataGridTextColumn Header="Роль" Binding="{Binding Role.Rolename}"/>
                                    <DataGridTextColumn Header="Логин" Binding="{Binding Userlogin}"/>
                                    <DataGridTextColumn Header="Пароль" Binding="{Binding Userpassword}"/>

                                    <DataGridTemplateColumn>
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Редактировать" 
                            Background="#4CAF50"
                            Foreground="White"
                            Command="{Binding DataContext.EditUserEvent,
                            RelativeSource={RelativeSource AncestorType=Page}}"
                            CommandParameter="{Binding}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn>
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Удалить"
                            Background="#F44336"
                            Foreground="White"
                            Command=""
                            CommandParameter=""/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>


                                </DataGrid.Columns>
                            </DataGrid>
                            <Button Command="{Binding CreateNewUserEvent}" Content="Создать пользователя" Width="150" Height="30" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10"/>
                        </Grid>

                    </TabItem>
                    <TabItem Header="Тикеты">
                        <DataGrid ItemsSource="{Binding TicketTable}" AutoGenerateColumns="False" ColumnWidth="auto" IsReadOnly="True" FontSize="14">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Имя" Binding="{Binding Fromuser.Firstname}"/>
                                <DataGridTextColumn Header="Фамилия" Binding="{Binding Fromuser.Lastname}"/>
                                <DataGridTextColumn Header="Отчество" Binding="{Binding Fromuser.Surname}"/>
                                <DataGridTextColumn Header="Дата" Binding="{Binding Ticketdate, StringFormat=dd.MM.yyy | HH:MM}"/>
                                <DataGridTextColumn Header="Описание" MaxWidth="500" Binding="{Binding Tickettext}">

                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="TextWrapping" Value="Wrap"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>

                                </DataGridTextColumn>




                                <DataGridTemplateColumn>
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Удалить"
                        Background="#F44336"
                        Foreground="White"
                        Command="{Binding DataContext.DeleteTicketEvent, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                        CommandParameter="{Binding}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                    </TabItem>
                </TabControl>

            </TabItem>
        </TabControl>



        <Rectangle VerticalAlignment="Top" Height="98.8" Fill="#FF194F9E" Stroke="#FF568FE2"/>
        <TextBlock TextWrapping="Wrap" FontSize="20" FontWeight="Bold" TextAlignment="Center" Foreground="White" Margin="0,23,0,0" VerticalAlignment="Top" HorizontalAlignment="Center"><Run Text="КОМПЬЮТЕРНАЯ СИCТЕМА ТЕСТИРОВАНИЯ МЧС РОССИИ"/><LineBreak/><Run Text="ПО ПОЖАРНОЙ БЕЗОПАСНОСТИ"/></TextBlock>
        <Rectangle Height="80" Width="80" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,9,0,0">
            <Rectangle.Fill>
                <ImageBrush ImageSource="/Assets/auto-891-1.png" TileMode="None" Stretch="UniformToFill"/>
            </Rectangle.Fill>
        </Rectangle>
        <Label FontSize="14" Content="{Binding WelcomeMessage}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="2,108,0,0" Width="217"/>
        <Button FontSize="14" Content="Выйти" Command="{Binding ExitEvent}" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,120,10,0" Height="30" Width="143"/>
    </Grid>
</Page>
